name: CFAA Blocklist Sync
# DISABLED: h3-provider is a provider library (package provider), not the
# connect library where ip_security_cfaa_block.go lives. CFAA blocklist sync
# belongs in urnetwork-3.23-fix or full-bars/connect, not here.
#
# Previously referenced .github/scripts/{write_sync_info.sh,cfaa_sync.sh,
# create_pr.sh,cfaa_sync_notify.py} which do not exist in this repo.

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why are you triggering this?'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::error::This workflow is disabled for h3-provider."
          echo "CFAA blocklist sync belongs in the connect library, not this provider repo."
          exit 1
