name: Release Binaries
# DISABLED: provider/ is package provider (library), not package main.
# This workflow cannot build a standalone provider binary.
# cmd/urnet-tools/ and cmd/urnet-docker/ do not exist in this repo.

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why are you triggering this?'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::error::This workflow is disabled for h3-provider."
          echo "provider/ is package provider (library), not package main."
          echo "cmd/urnet-tools/ and cmd/urnet-docker/ do not exist."
          exit 1
