name: Windows lifecycle verification
# DISABLED: requires cmd/urnet-tools/ and cmd/fake-provider/ (fork-specific CLIs).

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why are you triggering this?'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::error::This workflow is disabled for h3-provider."
          echo "cmd/urnet-tools/ and cmd/fake-provider/ do not exist in this repo."
          exit 1
