name: Dash/POSIX Compatibility
# DISABLED: requires scripts/Provider_Install_Linux.sh (fork-specific scripts).

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why are you triggering this?'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::error::This workflow is disabled for h3-provider."
          echo "scripts/Provider_Install_Linux.sh does not exist in this repo."
          exit 1
