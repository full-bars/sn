name: Pre-Release Shakedown
# DISABLED: requires .github/scripts/shakedown.sh (fork-specific script).

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why are you triggering this?'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::error::This workflow is disabled for h3-provider."
          echo ".github/scripts/shakedown.sh does not exist in this repo."
          exit 1
