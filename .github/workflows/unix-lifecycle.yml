name: Unix lifecycle verification
# DISABLED: requires cmd/urnet-tools/ (fork-specific CLI).

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why are you triggering this?'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::error::This workflow is disabled for h3-provider."
          echo "cmd/urnet-tools/ does not exist in this repo."
          exit 1
