name: docker-multi-container
# DISABLED: requires cmd/urnet-docker/ (fork-specific CLI).

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why are you triggering this?'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::error::This workflow is disabled for h3-provider."
          echo "cmd/urnet-docker/ does not exist in this repo."
          exit 1
