# Handoff

Future repository destination: `sim-testnet/peerreview/evidence/FINAL-2-afd7-preparation-20260914`.

The bundle is evidence-only. Its checksum manifest binds each derived public record to the closed input record(s) from which it was extracted. `SHA256SUMS` covers every bundled file except itself and its verification transcript; `SHA256SUMS.verify` records a successful verification.

Use this bundle to review closed preparation lineage, its separately labeled closed refusal, and proof limits. The refusal is evidence of exit `1`, not of a native submission, repair result, or final full-gate result.
