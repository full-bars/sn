# Exclusions

Not copied: binaries; caches; raw command records; stdout/stderr; capture launchers; private plans; configuration contents; keys; signed RLP; journals; live-state material; raw source/repository identity files; and the open native-approved-repair capture.

The bundle retains only derived summaries, selected public-safe review facts, source/CLI digest identities, closed exit/timestamp records, source mappings, checksums, and the expressly authorized public-safe refusal RESULT/CLOSURE/stderr records. The refusal request and all private plan material remain excluded.
