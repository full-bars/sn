# Reserve-repair succession qualification (portable raw evidence)

This package contains only the bounded 6311 qualification inputs and raw runner evidence. It excludes compiled test binaries, runner executables, Go caches, temporary directories, and vault/config contents.

The retained v1 and retry1 records are pre-body refusals. The accepted retry2 evidence records the expected old-source causal failure and corrected 30-root normal/race passes.
