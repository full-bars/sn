# Exclusions

Not copied: executable binaries, raw command scripts, caches, environment dumps, raw stdout/stderr build streams, native requests and plans, credentials, keys, signed payloads, journal/state data, live gate material, and source checkouts. These records remain only at their original closed locations and are listed by SHA-256 where relevant.
