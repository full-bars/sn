The next native failure is stale provisional staging output. Both operators have exactly three differences from the current resolved strict reserved policy: `provisional_seeded_discovery_only: true`, `provisional_retained_context_authority: true`, and four `activation_contexts`; the strict policy requires false, false, and an empty list. Every quota, owner bound, deployment pin, native runtime pin, and original companion discovery block matches. The current template and stale output both use the public native endpoint, so RPC routing was not the reported hash difference.

The current `st.yml` file hashes also differ from the preserved runtime manifest. That manifest still matches the original verified config-render receipt. The native audit authenticated the original receipt before failing its current-output check. These outputs require a new render; neither the stale provisional files nor a rewritten manifest may stand in for current strict verification. Exact field and hash evidence is in `CAUSAL-DIFF.json`.

The isolated source is `/home/by/urnetwork/temp/sn-runtime-render-convergence-20260914/sn`, commit `cf4eae3d70a83277494048e8d7cc44ea099b5c2d`, based on qualified `ca4281201077b6e3e9cde1004568efa219d0e12c`. `source-corrected.patch` contains the complete delta from the qualified base; the original `source.patch` and `CAUSAL-DIFF.json` retain the first candidate and diagnosis. The three production changes are:

- [runtime_config_manifest.go](/home/by/urnetwork/temp/sn-runtime-render-convergence-20260914/sn/sim-testnet/runtime_config_manifest.go:18) advances the established format marker to `strict-reserved-owned-rpc-v2`. Existing plan revision logic produces a new local render intent, so the ancestor render receipt cannot suppress current rendering.
- [plan.go](/home/by/urnetwork/temp/sn-runtime-render-convergence-20260914/sn/sim-testnet/plan.go:1557) binds `config.render` to the already approved resolved-input hash. Future owned-route changes must also rerender, even when consent/config identity remains unchanged.
- [runtime_reserved_attempt_upload.go](/home/by/urnetwork/temp/sn-runtime-render-convergence-20260914/sn/sim-testnet/runtime_reserved_attempt_upload.go:108) selects the existing `ws://127.0.0.10:19946` native proxy for reserved discovery before full server deployment validation, while preserving the canonical source template and every capacity/pin. The native provisioning process specification forwards that proxy to `192.168.1.162:9944` without pacing or public fallback. Without this correction, rerendering would leave that server reader on the public native endpoint.

No executor migration framework, relaxed verification, source-config edit, or state/journal/receipt edit was introduced. The existing native render writes new runtime outputs and applies its full current postcondition. The historical platform-config override remains required. Root has already established that this actual deployment is stopped; retain that stopped condition before applying the new local render.

Terra owns formatting, normal/race execution and build. The same bounded selector was handed over for both normal and race runs in `./sim-testnet`:

```text
^(TestSimulatorClientSeedCustodyWalletRevisionRequiresFreshRender|TestOwnedRPCPlanApprovalBindsExactRouteWithoutChangingCustody|TestOwnedRPCStrictRoutePreservesConsentAndUnlimitsOwnedTransport|TestRuntimeEvidenceLaunchV2TemplateReachesGeneratedSetupAndRender|TestRuntimeEvidenceOwnedReservedStagingRerenderReplacesProvisionalConfig|TestRuntimeEvidenceV2ReservedRendererRejectsMissingOrChangedSetup|TestRuntimeEvidenceLaunchV2TemplateRejectsMissingAndForeignCreation)$
```

The existing render-format regression now covers the actual prior `signed-provider-wallet-v1` format. It verifies unchanged chain-action intents, policy, custody and spending limits, rejects prior-render intent reuse, and requires fresh rendering. The new owned renderer regression uses the existing generated-contract/source fixture, injects the actual provisional flags, four original contexts and public native URLs into both output files, requires `runtime reserved staging differs` before rerendering, then verifies the strict owned outputs and exact manifest. It also checks unchanged original plan, journal, creation receipt, prepared/completed evidence and miner payout seed. The existing missing/foreign source controls still fail before renderer mutation.

The previous full producer 36/36 and aggregate 25/25 results remain evidence for the unchanged `ca42812` source graph. The new source delta requires the bounded checks and fresh native CLI/release-lock authentication; do not describe the old full results as execution against this new commit. Server, contracts, validator implementation, transport packages, capacity/config policy and the qualified 13-repository graph were not changed. Root can retain those unchanged results and review the new native plan's action/spend delta before the already authorized repair. Actual qualification and retry results are pending Terra/root; Astra ran no tests, build or formatter.

The first bounded normal run on `1469b14` failed full server admission because direct plain WebSocket access to a private LAN address is outside its TLS-or-literal-loopback policy. `CORRECTION.json` records the exact capture and correction in `cf4eae3`. The unchanged server policy remains enforced, and the regression now also checks the actual provisioning proxy specification. Terra owns the corrected rerun.
