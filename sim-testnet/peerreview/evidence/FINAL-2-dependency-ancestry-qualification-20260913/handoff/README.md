Dependency ancestry admission — frozen source handoff v1

Source 17286251723b05a00717a642a6b5141cb02c2e12, parent published e0a454248051a24d81c11d166054570cb4d20b5e.
Physical isolated source: /home/by/urnetwork/temp/sn-dependency-ancestry-admission-20260913/sn.
Only two files change: scripts/check-release-source-freeze.sh and sim-testnet/release_gate_test.go.

The current gate compared every dependency checkout to the latest canonical branch HEAD after a fresh fetch. Independent upstream advancement therefore invalidated a clean unchanged tested dependency. The correction keeps exact freshly fetched origin/main equality for SN and requires every dependency HEAD to be an ancestor of its freshly fetched canonical main/master instead. Missing, dirty, wrong-origin, wrong-upstream, unpublished, divergent and rewound-out dependencies still refuse. No new flags, source pins, caches, owner framework, inventory/module changes or deadlines.

The existing four-column snapshot still records the checked-out commit, upstream name and canonical origin. It does not substitute the newer remote commit. The in-command initial/final snapshot comparison and both gates' final comparison remain unchanged, so the actual tested checkout cannot change unnoticed while upstream advances.

Behavioral regression: TestReleaseSourceFreezeRetainsPublishedDependencyWhenUpstreamAdvances exercises both origin/main and origin/master using local fixture remotes, confirms exact baseline snapshot/HEAD retention and actual upstream refresh, then rejects a previously published commit removed by a rewind or divergent branch. The existing stale-remote regression now uses SN and retains strict latest-main refusal; existing Warp/unpublished negatives expect the precise ancestry refusal. No descendants were added. The pre-change script would refuse the first dependency-ancestor positive case; this is source analysis, not an executed causal control.

Native review found no additional dependency HEAD==remote requirement. sim-testnet/executable_attestation.go:pushedSNRevision and validateReleaseExecutableIdentity concern SN only and remain unchanged. Dependency source admission in release_lock.go observes clean source digests/commits; release_lock_render.go brackets them with clean snapshots and rejects snapshot change. Native SN still requires the published current executable revision, so the normal final source publication/CLI stamp rules remain.

Terra: run the exact integration12 normal and race selections once, count1/parallel4/test600/build1200/owner660/jobs1/GOMAXPROCS4 under the existing qualification helper. All outcomes are sorted uppercase PASS; literals empty; no descendants; no new R3 obligations. new1 is a subset for review, not an additional required repeat. Preserve the already qualified private 13 dependency HEADs and byte/mode manifests; do not follow the newly advanced dependency tips. Root owns final publication and source-pair binding. The helper plan must retain the accepted numeric version1, source_root, all 13 sources, limits, package declaration and suite declaration. Update only the isolated SN source proof and this exact test selection; existing source/environment ownership rules remain.

Existing completed producer, capture, reserve repair, fixture and dependency qualification remains on its recorded scope. This script/test-only change does not reopen disjoint proof streaks. Final producer/aggregate gates use their already prepared owners after Root binds the final source. No Go, build, test, formatter, evidence replay, chain write, source-world mutation or process signal was executed by Astra; git diff --check passed.
