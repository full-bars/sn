# Owned-RPC throttle correction evidence

This external bundle separates the qualified source-rendered nginx correction from the unchanged physical host and from the two closed native refusals. It contains source patches, deterministic Jinja outcomes, a source-rendered candidate artifact, and derived public-safe terminal records.

It does not establish the deployed nginx configuration, which limiter kind produced either 429, successful rollout, or native-repair completion.
