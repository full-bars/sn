# Exclusions

This compact bundle excludes private plans and configurations, request payloads, raw stdout/stderr, signed payloads, keys, RLP, journals, identities, binaries, caches, live host files, SSH credentials, and raw native captures. Closed raw terminals and native records are hash-referenced; public-safe derived outcomes are retained.
