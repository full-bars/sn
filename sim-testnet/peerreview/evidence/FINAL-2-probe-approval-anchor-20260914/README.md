# FINAL-2 probe approval anchor qualification evidence

Intended repository destination: `sim-testnet/peerreview/evidence/FINAL-2-probe-approval-anchor-20260914`.

This compact external bundle records the signed-checkpoint correction’s deterministic regression, adjacent controls, exact normal/race membership and outcomes, required normal confirmation streak, formatter identity, and source/binary/dependency fences. Verify retained files with `sha256sum -c SHA256SUMS`.

Raw streams, commands, source manifests, binaries, caches, native requests, plans, configurations, keys, RLP, live captures, and full-gate material are not copied. `references/RAW-HASH-REFERENCES.tsv` retains their identities by SHA-256.
