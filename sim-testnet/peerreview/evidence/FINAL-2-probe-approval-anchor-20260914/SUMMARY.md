# Probe approval anchor qualification

The original apply refusal was caused by a freshly constructed descriptor binding its approval hash to a moving finalized head. The correction anchors that descriptor to the authenticated signed repair checkpoint, while still checking current custody before an unconsumed replacement create.

The exact 40-root normal and race matrices each closed with 40 top-level PASS terminals and a passing package terminal. Each stream contained 116 descendant **events**. This bundle does not convert those events into a descendant PASS count; the claimed result is exactly 40 top-level passes per mode.

`TestPrecompileProbeApprovalSurvivesAdvancingFinality` then passed three fresh normal processes on the same normal binary/source: the full normal matrix p1 and focused p2/p3. The disposable moving-head causal restored only `FinalizedHead: head`; it closed with the required one expected FAIL and three PASS controls. It is causal evidence, not a candidate failure.

The original native refusal remains exit 1 before application, with unchanged state and zero new transactions. Its closed timestamps are in `native/ORIGINAL-REFUSAL-REFERENCE.tsv`; no live preview or full-gate result is claimed here.
