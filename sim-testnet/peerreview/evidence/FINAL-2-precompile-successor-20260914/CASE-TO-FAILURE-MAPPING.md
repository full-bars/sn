# Deterministic case-to-failure mapping

| Observed closed case | Original outcome and bounded cause | Corrected or adjacent case | Closure recorded here |
| --- | --- | --- | --- |
| `TestPrecompileProbeSuccessorConstructsOriginalNativeReplay` | Failed in both 4f59 matrices. The fdff diagnostic then reported `wire_equal=true`: an in-memory `DecimalUint("")` differed structurally from the persisted archive's canonical `DecimalUint("0")` despite identical signed wire data. | 511 reloads the persisted approval before deriving the successor. `TestPrecompileProbeSuccessorRetainsCanonicalArchivedAmounts` changes each relevant zero amount and requires rejection. | Final normal/race 8-root p1 passed; constructor normal p1/p2/p3 and race p1/p2/p3 passed. |
| `TestReleaseGatesAttestPinnedRuntime454RustSource` | Failed on 4f59 because its test still required `expected_current_files=31` while the reviewed runtime455 checker/manifest were already 33. | 511 centralizes the 33-file count. | Final normal/race p1 passed; the original stale gate root passed normal p1/p2/p3. |
| `TestReleaseGatesAttestRuntime455SourceCompatibility` | Failed on 4f59 because its local comparison map omitted `precompiles/src/address_mapping.rs` (and the paired runtime455 source row). | 511 validates each reviewed path/digest exactly once; `TestReleaseGatesAttestRuntimeSourceCensusRejectsDrift` covers omission, duplicate, substitution, malformed, and empty census inputs. | Final normal/race p1 passed; the original stale gate root passed normal p1/p2/p3. |
| Causal `KeepsCoreExecutableChecks` and `RerendersAfterValuePhases` | In a disposable 4f59 mutation that restores the old artifact and CREATE-only nonce assumptions, these two roots failed with their required literals. | `PinsSingleCreateBoundary` and `NeverResendsNativeDrill` remain controls on that same causal input. | Expected 2 FAIL / 2 PASS only; this remains a causal control, not candidate evidence. |
| Original 22 selected new roots (21 successor +1 battery) | The exact 21 `TestPrecompileProbeSuccessor*` roots plus the deferred battery root were selected in the original 4f59 matrix. They provide adjacent coverage but include the constructor failure above. | Final511 separately adds `TestPrecompileProbeSuccessorRetainsCanonicalArchivedAmounts` as the 22nd successor root in its eight-root matrix. | `source/successor-22-roots.sorted`, the paired original normal/race outcome table, and final511 membership preserve the distinction. |

This mapping records deterministic test causes and adjacent controls under the repository's test policy. It does not relabel an earlier failure as a pass or extend the final 511 scope beyond its eight selected roots and narrow confirmations.
