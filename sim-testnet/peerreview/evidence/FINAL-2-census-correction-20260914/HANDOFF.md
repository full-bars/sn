Capture census correction is frozen at local commit `43cc2b21811d606c58fc5aa4bb3a977ddefaef2f`, parent `a59294e98ea02d05125015ae02cf32f2c0059c8a`, in `sn/`. The worktree is clean. Only `sim-testnet/release_gate_capture_metadata_test.go` and `sim-testnet/fleet_renewal_oracle_test.go` changed. Astra performed source inspection and `git diff --check`, and did not run Go, tests, builds, formatters, RPC, native operations or pushes. Active primary and physical dependency sources were untouched.

The cause is a fixed-size assertion for an intentionally growing source family. Static declaration inspection against the actual producer selectors gives:

| Source | Ordinary | Separate | Total | Ownership errors |
| --- | ---: | ---: | ---: | ---: |
| Parent of `536dba6` | 343 | 111 | 454 | 0 |
| `a59294e9` | 345 | 111 | 456 | 0 |
| `43cc2b2` | 348 | 111 | 459 | 0 |

The only added selected declarations between the first two rows are `TestFleetRenewalOriginalOracleAcceptsCompletedRestore` and `TestFleetRenewalOriginalOracleRejectsChangedRouting`; none were removed. Both match the capture family's `FleetRenewal` alternative, neither matches the finite separate renewal/revision patterns, and neither matches ordinary capture's skip. Thus each correctly runs under the ordinary owner. The two retained raw failure logs report precisely 345 ordinary and 456 total at the old assertion:

- `/home/by/urnetwork/temp/sn-renewal-oracle-correction-20260914/terra-runtime/producer-a59294e9-globalmodcache/tmp/urnetwork-release-gate.VofNMNMu/logs/capture.log:7`
- `/home/by/urnetwork/temp/sn-renewal-oracle-correction-20260914/terra-runtime/aggregate-a59294e9-globalmodcache/tmp/urnetwork-release-gate.vrWmbsWA/logs/sn-all-normal.log:230`

The correction pins the complete family selector independently of the script's selector and retains the existing exactly-one-owner check on every selected declaration, source duplicate rejection, finite cohort counts, exact registrations, skips, commands and deadlines. The evidence family had the adjacent fixed-87 defect: its count now comes from all evidence declarations minus its two independently required stress owners. Ordinary and overall totals come from the actual complete source population and its owner union. Both oracle regressions are explicitly required to keep their ordinary owner.

Three new deterministic top-level roots exercise two additional synthetic ordinary declarations, one additional synthetic evidence declaration, and missing/duplicate retained declarations plus omitted source families. Existing metadata/population mutation tests keep covering omitted and repeated execution owners, broad skips, hidden/conditional jobs, false-success commands and deadline changes. All original roots remain selected.

The oracle test file now follows the user-specified primary `connect/CODESTYLE.md`: synthetic addresses, block and epochs; a plain homogeneous rejection loop with named struct fields; concise file/function comments. The same nine rejection cases, no-schedule acceptance, exact effective-epoch boundary, five-element pinned batch and both root names remain. Production planning and per-write checks still invoke the unchanged `fleetRenewalOriginalOracleReady` at `fleet_renewal_observe.go:325` and `fleet_renewal_execute.go:482`.

Terra should first bind the same admitted dependency/module graph in its private qualification workspace. This bare SN worktree does not prepare sibling module replacements. The focused selector below has 21 top-level source roots on this commit; Terra must verify that against the compiled list. It includes all capture-selection guards (including the adjacent offline-analysis guard), both oracle regressions, five complete-family source guards and both aggregate partition guards.

```sh
capture_census_tests='^(TestProducerGateCaptureSelection.*|TestFleetRenewalOriginalOracle(AcceptsCompletedRestore|RejectsChangedRouting)|TestProducerGateStateSelection(CoversEvidenceV2OwnGuardSources|EvidenceV2(PreservesEveryOldFamily|RejectsEveryNewFamilyOmission|KeepsAdjacentSourceCoverage|RequiresCompleteSourceCensus))|TestReleaseGateJobs(RequireIndependentCompleteValidatorRace|RejectSimulatorPopulationPartitionDrift))$'
go test ./sim-testnet -run "$capture_census_tests" -count=1 -parallel=4 -timeout 5m
go test -race ./sim-testnet -run "$capture_census_tests" -count=1 -parallel=4 -timeout 10m
```

Use the established build-once qualification owner for confirmations: compile one private test binary per mode, retain build/list/source evidence, and execute that binary from `sim-testnet/` with the same selection, timeout, parallelism and count settings. The known failed root is `^TestProducerGateCaptureSelectionRequiresIndependentPopulation$`. It needs three sequential fresh normal processes on the same source/dependencies/binary. If its race failure was also observed, require the corresponding three race processes. The first focused execution may count toward its relevant streak when the binary and all input fences are identical. Do not count cache reuse or executions on other revisions.

For deterministic causal validation, prepare separate immutable test-only variants of this commit, never change the fixed or active sources:

1. In `verifyReleaseGateCaptureSourceCensus`, append fixed predicates calibrated to the unchanged candidate: `|| counts["capture_evidence_tests"] != 87 || len(ordinaryOwners) != 348 || len(selected) != 459` to the final census condition. Keep the source parsing and all new tests. Run `^TestProducerGateCaptureSelection(RequiresIndependentPopulation|AdmitsAdditionalOrdinarySources|AdmitsAdditionalEvidenceSources)$`. The unchanged-source control must pass; the two additional-source roots must fail with the root-owned literal `capture partition changed its complete source census`. The static expected ordinary/total counts are 348/459, 350/461 and 348/460 respectively; the last has 88 evidence owners. This directly proves that updating the old totals to today's cardinality leaves the root cause intact. The original 343/454 failure is already established by the preserved a592 gate executions; no blanket old-source rerun is needed.
2. To isolate the adjacent evidence defect, append only `|| counts["capture_evidence_tests"] != 87` to that same condition. Run the same three roots. The unchanged-source and additional-ordinary controls must pass; only `AdmitsAdditionalEvidenceSources` must fail with the same census literal. On fixed `43cc2b2`, all three must pass. Do not treat either causal variant as release qualification.

Adjacent source searches covered `sim-testnet/release_gate*test.go`, particularly `release_gate_capture_metadata_test.go`, `release_gate_evidence_v2_test.go:161`, `release_gate_jobs_test.go:688`, `release_gate_test.go:121`, both producer/aggregate scripts, `fleet_renewal_oracle_test.go` and both oracle production call sites. The only source-population cardinality assertions of the failing shape were the ordinary/total and evidence counts corrected here. Fixed counts for named finite cohorts remain deliberate. Aggregate partition verification already derives complete membership and checks the actual registered commands and owner selectors; it had no matching stale total to change. Existing complete-source guards include `fleet_renewal*_test.go`, so both oracle roots remain covered there too.

Reuse and identity limits are explicit in `FINALIZE.md:8` and `sim-testnet/README.md:85`: completed disjoint evidence/streaks remain valid only for their recorded source/dependency scope; final acceptance still requires both complete gates on the final candidate. No selective full-gate carry/resume mechanism was found in the actual gate scripts/owner. The existing qualification `replay` mechanism only rechecks immutable events for a checker-only refusal; it cannot pass this executed assertion failure.

Both changed paths are excluded by `classifyGoReleasePath` (`release_lock.go:353`), and `_test.go` also remains excluded from the protocol digest (`release_lock.go:320`). Therefore production-source and release-lock bytes need not change for this patch. The lock file SHA-256 remains `ea407397566b247e67c66a318fa647c4151db60cdb923e7583d0f3a16cc9ac2f`. All other tracked files are unchanged from `a59294e9`. Qualification snapshots and VCS identity nevertheless change: `observeCleanGoReleaseSource` binds the digest to a clean commit, executable attestation compares executable/check-out/canonical revisions (`executable_attestation.go:355`), and both gate scripts compare initial/final complete source snapshots. An old stamped executable is not automatically authenticated against a newly committed checkout merely because its production digest matches. Keep the live frozen source world intact while root performs integration through the existing reviewed process.
