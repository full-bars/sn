Enumeration correction is frozen at local commit `ff62e1cae9cfa831e00481a0dad98cece75f1143`, parent `43cc2b21811d606c58fc5aa4bb3a977ddefaef2f`, in `sn/`. The commit was created at 2026-09-14 08:59:24 UTC and the worktree is clean. Only `sim-testnet/final_semantic_producer_test.go` changed: mode `100644`, Git blob `ce9ab5cf3e67c6fd879dab20a62ee610dd5f9753`, SHA-256 `0a4a7c172c4ad2862b557cd807dfaf35c7e0d4d682169df09cef532703ca5303`. The original 43cc checkout remains clean with its original census/oracle hashes. Primary and the live physical source world were untouched.

The retained normal 43cc focused body started at 08:45:57 UTC and ended at 08:48:08 UTC with body exit 1. `TestProducerGateCaptureSelectionExcludesOfflineSemanticAnalysis` took 120.02 seconds and failed with `enumerate producer gate capture tests: signal: killed`. Its source used `context.WithTimeout(..., 2*time.Minute)` around `exec.CommandContext(ctx, "go", "test", "-list", selection, ".")`. It was recursively asking the Go tool to compile/list the package whose binary was already running. The later race body passed all 21 roots, including this root in 10.02 seconds. Exact internal compiler activity or an OOM was not established; the proven defect is the unnecessary compiler/cache/toolchain dependency under a listing deadline. The normal failure and later race pass retain their original scopes.

Raw original evidence is under `/home/by/urnetwork/temp/sn-capture-census-correction-20260914/terra-runtime/capture-focused-20260914T084049Z/{normal,race}/`, including `run.meta`, `raw/test2json.raw`, and `events.json`. Both source/binary fences and conversions passed. All corrected census and oracle roots passed in each body; that does not pass the failed overall normal body.

The correction uses `os.Executable()` and `-test.list=<selector>` to enumerate the exact running test binary. `CommandContext` and `CombinedOutput` retain direct-child cancellation and waiting; there is no compiler subprocess tree. The existing two-minute limit is unchanged, and the parent test context now also controls the child. The complete sorted compiled capture population must exactly equal the existing source-declaration census before the unchanged nine required roots, forbidden analysis families and breadth floor are checked. The selector reader now uses the existing unique-assignment parser. Duplicate, missing or foreign compiled names fail the exact census comparison.

Three deterministic top-level regressions were added, following the user-specified `connect/CODESTYLE.md`:

- `TestProducerGateCaptureSelectionListsCurrentBinaryWithoutGoTool` sets the parent test process's PATH to an empty synthetic temporary directory and requires an exact one-root compiled list. An inherited marker causes that root to fail immediately if a listing ever executes its body. It is deliberately nonparallel so `t.Setenv` owns/restores process environment safely. Setting only a prospective child's Env would not test Go lookup: `exec.CommandContext` resolves the command before child Env is applied.
- `TestProducerGateCaptureSelectionEnumerationHonorsCanceledContext` supplies an already-canceled owner and requires cancellation with no roots.
- `TestProducerGateCaptureSelectionEnumerationRejectsEmptySelector` requires an explicit empty-selector refusal before subprocess dispatch. Local `testing/testing.go` has an early listing return only for a nonempty `-test.list`; an empty list flag could otherwise execute the suite. This regression also uses an already-canceled owner, so removing the guard fails deterministically without executing bodies or waiting for a deadline.

The adjacent search covered executing Go commands and all `-list`/`-test.list` occurrences throughout repository Go source, the shell gate scripts, `sim-testnet/main_test.go`, and existing reexecution/cancellation fixtures. This was the sole executing nested Go-test enumeration in the package. `scripts/qualification/main.go:653` already lists its admitted binary directly. `process.go:1007` is an actual production build, and the producer script's semantic census at line 187 is a distinct release compile/list command; neither was changed. The package's `TestMain` delegates to `m.Run` except for its existing unrelated stdio probe. Local toolchain source `/usr/local/go/src/testing/testing.go:2407` confirms listing returns before test bodies. No new child owner, service, runtime control, RPC or native mutation was introduced.

Terra owns formatting, compilation and all execution. Formatter scope is only `sim-testnet/final_semantic_producer_test.go` in the new successor. The two unchanged 43cc census/oracle files retain their existing no-delta formatting proof. Admit the same complete private dependency/module graph, compile one private binary per mode, record the exact compiled membership, and retain all body/owner/conversion/fence results.

The previous focused selector now has 24 source roots. The current full capture population is expected to be 462 roots: 351 ordinary plus the unchanged 111 separate owners. These are inspection expectations for Terra to verify against actual compiled lists, not execution results.

```sh
capture_census_tests='^(TestProducerGateCaptureSelection.*|TestFleetRenewalOriginalOracle(AcceptsCompletedRestore|RejectsChangedRouting)|TestProducerGateStateSelection(CoversEvidenceV2OwnGuardSources|EvidenceV2(PreservesEveryOldFamily|RejectsEveryNewFamilyOmission|KeepsAdjacentSourceCoverage|RequiresCompleteSourceCensus))|TestReleaseGateJobs(RequireIndependentCompleteValidatorRace|RejectSimulatorPopulationPartitionDrift))$'
go test ./sim-testnet -run "$capture_census_tests" -count=1 -parallel=4 -timeout 5m
go test -race ./sim-testnet -run "$capture_census_tests" -count=1 -parallel=4 -timeout 10m
```

Use the admitted build-once qualification owner rather than repeating compilation for confirmations. The original `TestProducerGateCaptureSelectionRequiresIndependentPopulation` normal streak is COMPLETE on the 43cc binary/source: pass 1 took 0.64 seconds in the original focused body, pass 2 ran at 08:54:14–08:54:15 UTC (0.70 seconds), and pass 3 at 08:55:50–08:55:51 UTC (0.82 seconds), with all fences passing. Root confirmed the retained evidence at `/home/by/urnetwork/temp/sn-capture-census-correction-20260914/terra-runtime/capture-focused-20260914T084049Z/confirmations/requires-independent-normal-p{2,3}`. Preserve that completed scoped streak; the successor 24-root matrix supplies affected integration coverage and does not reopen it.

Only the newly failed `TestProducerGateCaptureSelectionExcludesOfflineSemanticAnalysis` needs three consecutive fresh normal executions on one final source/dependency/binary snapshot. The initial focused successor pass may count as pass 1 when all identities and fences match. Its exact confirmation selector is `^TestProducerGateCaptureSelectionExcludesOfflineSemanticAnalysis$`. The observed enumeration failure is normal-mode only; one affected successor race run is required, with additional three-pass race obligations only if an actual race failure is separately recorded. Preserve completed disjoint source-scoped work.

Root selected this accelerated deterministic pre-fix proof instead of compiling a new old-command mutant: reuse Terra's already-built and hash-verified 43cc NORMAL test binary, from its frozen 43cc `sim-testnet/` package directory. Run only `^TestProducerGateCaptureSelectionExcludesOfflineSemanticAnalysis$` with the TEST BINARY CHILD's PATH set to an empty synthetic directory. Invoke the binary by absolute path; keep ownership/capture/converter tooling on its normal environment. Expected result is an immediate original-root failure containing `enumerate producer gate capture tests` and `executable file not found in $PATH`. Retain the old source/binary identity and exact environment change. This directly reproduces the original dependency without another compiler or two-minute wait. Do not relabel that intentional causal failure as qualification. Fixed ff62e1c's complete root and no-Go/body-canary/cancel/empty regressions then establish the correction. No optional command-mutant builds are needed absent a new concern.

The earlier census causal work may still use its separate frozen 43cc source and already-built binaries. If instead Terra creates its fixed-count causal variants from this newer source, recalibrate ONLY those deliberate mutants to 351 ordinary / 462 total / 87 evidence: unchanged-source control passes, added ordinary roots give 353/464 and fail, added evidence gives 351/463 with 88 evidence owners and fails. Never edit a qualified or live source world to create a mutant, and never put those fixed counts into the repair itself.

This remains a test-only correction. The release-lock file SHA-256 is unchanged at `ea407397566b247e67c66a318fa647c4151db60cdb923e7583d0f3a16cc9ac2f`; production and protocol source hash classifiers exclude `_test.go`. Clean Git/test-binary/qualification identities still change. Completed disjoint evidence keeps only its recorded scope, and both complete final gates remain required; no phase-carry mechanism or retrospective full-gate pass is asserted. Astra ran only source inspection and `git diff --check`, with no Go invocation, tests, build, formatter, RPC, native state write or push.
