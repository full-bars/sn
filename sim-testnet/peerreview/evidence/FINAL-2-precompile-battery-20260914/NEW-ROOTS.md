# New Solidity root membership

`membership/new-solidity-roots.tsv` records the exact seven new roots. The live-failure regression `test_readBattery_runtime455RejectsLegacyBlake2f` has three separate fresh normal passes (p1–p3); each adjacent root has one selected-body pass. All bodies used clean `d7142cc`.

Two earlier zero-test selector observations are retained only as non-qualifying command-admission records. They are neither source failures nor qualifying passes.
