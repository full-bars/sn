# Exclusions

The bundle excludes private plans and configurations, request payloads, raw stdout, signed payloads, keys, RLP, journals, identities, binaries, caches, raw live captures, and the live retry. It retains the closed failure message as a derived public-safe line and hashes raw closed records instead of copying them.
