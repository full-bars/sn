The existing final 7eab CLI can render the coherent successor lock directly. No SN bootstrap build is required if root reviews and installs the exact read-only rendered YAML as a repository change, then builds the final published revision once. The CLI's `release-lock --apply` form cannot use the old executable after SN advances: source-only attestation still requires binary revision = checkout revision = current pushed `origin/main`. Read-only rendering selects no executable attestation and returns canonical YAML to stdout ([executable_attestation.go:67](/home/by/urnetwork/sn/sim-testnet/executable_attestation.go:67), [executable_attestation.go:341](/home/by/urnetwork/sn/sim-testnet/executable_attestation.go:341), [release_lock_render.go:588](/home/by/urnetwork/sn/sim-testnet/release_lock_render.go:588)). The atomic CLI apply workflow documented in README is available, but its intermediate build is unnecessary for this rendered-file integration route.

This review read source and reduced retained-state fields only. It did not execute the CLI, tests, builds, RPC, or source/state mutations. Source citations refer to the still-frozen 7eab checkout; the joined SN commit changes none of these implementation files.

The current joined inputs are SN `65af88a5d6ce34f63b36de5fbbda98075eac20bf` and xops `d33d4173767ad9dab2f301bc0b0e040579bee896` in `/home/by/urnetwork/temp/sn-infrastructure-release-integration-20260914`. The xops Subtensor inputs are byte-identical to qualified `51325d00e18a157b2ceb5d5ac44bb0645550b289`; the other reviewed release dependencies remain unchanged. Apart from reports/evidence, SN differs from 7eab in `evm/test/SP1Probe.t.sol`, the producer script and four Go regression files. These changes leave the production Go and Solidity source digests unchanged: the Go digest excludes `_test.go` and shell files, while Solidity covers `evm/src` and `evm/script`. However, **the producer shell script is explicitly included in the separate protocol digest** ([release_lock.go:325](/home/by/urnetwork/sn/sim-testnet/release_lock.go:325), [release_lock.go:552](/home/by/urnetwork/sn/sim-testnet/release_lock.go:552), [release_lock.go:682](/home/by/urnetwork/sn/sim-testnet/release_lock.go:682)).

The old 7eab CLI remains valid for this pure local render: the joined checkout has no diff in `release_lock.go`, `release_lock_render.go`, executable attestation, main/config dispatch or `contracts_gen.go`. The renderer reads the current producer script from `cfg.Repos.SN` when calculating `repositories.protocol_source_hash`; it does not reuse a compiled old protocol digest. Generated artifact identities are compiled constants, and those constants remain exact here ([release_lock.go:865](/home/by/urnetwork/sn/sim-testnet/release_lock.go:865), [release_lock.go:916](/home/by/urnetwork/sn/sim-testnet/release_lock.go:916)). This conclusion does not authorize an old binary to render a future changed contract artifact.

The earlier two-field expectation omitted that protocol input. Expect **three** lock-field changes when the remaining inputs/tools are exact: `repositories.protocol_source_hash` from the producer schedule, `infrastructure.gateway_config_hash` from the changed playbook/nginx template, and `infrastructure.node_config_hash` from changed `vars.yml`. The producer script is the sole changed input in the protocol hash's explicit source list for this joined candidate. Review the actual rendered diff before installation; this note does not claim an observed new lock digest ([release_lock.go:682](/home/by/urnetwork/sn/sim-testnet/release_lock.go:682), [release_lock.go:696](/home/by/urnetwork/sn/sim-testnet/release_lock.go:696), [release_lock.go:812](/home/by/urnetwork/sn/sim-testnet/release_lock.go:812), [release_lock.go:943](/home/by/urnetwork/sn/sim-testnet/release_lock.go:943)).

The minimal coherent sequence is:

1. Let existing consumer owners close, or prepare a separate physical projection. Join the qualified SN and xops commits with the other exact dependencies in clean worktrees. xops must be the sibling `xops` of the selected SN root; there is no xops override flag. The renderer brackets all contributing repositories and the three Foundry dependency worktrees with clean snapshots. Preserve the existing unlocked vault/config overrides and pinned Forge/abigen tools. `--vault-repo` selects `cfg.Repos.Vault` directly; a new encrypted vault worktree is unnecessary when the explicit override names the existing approved unlocked physical vault ([release_lock_render.go:170](/home/by/urnetwork/sn/sim-testnet/release_lock_render.go:170), [release_lock_render.go:251](/home/by/urnetwork/sn/sim-testnet/release_lock_render.go:251)).
2. Run the existing 7eab binary's read-only `release-lock`, saving stdout outside every source checkout. Review the three-field change above, verify the inputs stayed frozen, install those exact candidate bytes with the lock's existing mode, and commit/publish the final SN source plus lock. No native preview is needed before the lock is final. The renderer invokes only local source/tool identity observations; abigen `--preflight` returns before generation ([release_lock_render.go:505](/home/by/urnetwork/sn/sim-testnet/release_lock_render.go:505), [release_lock.go:655](/home/by/urnetwork/sn/sim-testnet/release_lock.go:655), [stabi/generate.sh:98](/home/by/urnetwork/sn/stabi/generate.sh:98)).
3. Terra builds one final canonical CLI from that clean published revision and qualifies the complete final source/dependency pair. Preserve completed focused evidence within its exact scope and the old gate failures; the old 7eab/xops-a9 gates cannot establish a PASS for the new pair ([FINALIZE.md:9](/home/by/urnetwork/sn/FINALIZE.md:9)). A lock-only commit does not alter the already-qualified Go/Solidity source bytes, but the final native binary must carry the new VCS revision.
4. With that final CLI and the unchanged native config/state/owned-RPC arguments, take one `setup` preview. Review its exact successor plan and apply that hash once. `setup` already invokes the revision doctor and fresh facts; a separate `plan` or bootstrap preview adds no required stage. Apply independently repeats admission and checks the hash before installing run inputs ([executor.go:530](/home/by/urnetwork/sn/sim-testnet/executor.go:530), [executor.go:558](/home/by/urnetwork/sn/sim-testnet/executor.go:558), [plan_revision.go:4324](/home/by/urnetwork/sn/sim-testnet/plan_revision.go:4324)).

Command shapes, for root to execute only after the projection is ready: set `joined_sn` to its physical SN root, and `repo_args` to the same five repository overrides in the retained [REQUEST.json](/home/by/urnetwork/temp/sn-probe-approval-anchor-correction-20260914/native-approved-repair-7eab/REQUEST.json), replacing source roots with the joined projection while preserving the approved vault/platform-config inputs. `repo_args` must also include `--config "$joined_sn/sim-testnet/testnet.yml"`. The lock command accepts no state directory, owned-RPC flag, or JSON format flag.

```bash
/home/by/urnetwork/temp/sn-probe-approval-anchor-correction-20260914/terra-runtime/final-cli-probe-approval-anchor/sim-testnet release-lock "${repo_args[@]}" > /home/by/urnetwork/temp/sn-rpc-gateway-unpaced-20260914/release.lock.candidate.yml
```

After the final source/lock commit and canonical build, use the final binary's absolute path as `final_cli`; set `reviewed_successor_hash` from the single reviewed preview. These are command pointers, not actions executed by this review:

```bash
"$final_cli" setup "${repo_args[@]}" --state-dir /home/by/urnetwork/sn/sim-testnet/runs/ur-subnet-testnet-v1-attempt-4 --owned-rpc-authority 192.168.1.162:9944 --format json
"$final_cli" setup "${repo_args[@]}" --state-dir /home/by/urnetwork/sn/sim-testnet/runs/ur-subnet-testnet-v1-attempt-4 --owned-rpc-authority 192.168.1.162:9944 --format json --apply --plan-hash "$reviewed_successor_hash"
```

The installed plan is `0x2b5527989bd2ca38b58d3046e82f2d7fdb5ab1b5b86f3af74ba58e5957ced95d`, with release-lock hash `0x52a046256b52152373a92ff3dbf38518f181b3ec44a74bdac84577d84d7a4d6a`. A changed infrastructure lock necessarily changes the new plan hash: `ReleaseLockHash` is hashed and is compared when loading the active plan. `BuildPlanForState` then takes the authenticated prior-plan revision path ([plan.go:830](/home/by/urnetwork/sn/sim-testnet/plan.go:830), [plan.go:1982](/home/by/urnetwork/sn/sim-testnet/plan.go:1982), [executor.go:739](/home/by/urnetwork/sn/sim-testnet/executor.go:739), [plan_revision.go:4410](/home/by/urnetwork/sn/sim-testnet/plan_revision.go:4410)). The old 2b552 hash cannot approve that successor unchanged.

This infrastructure change does not itself enter action intent hashes. Revision retains allowed ancestry, exact completed renewal actions/lifecycle consent, original evidence source locks, and the existing probe successor descriptor; it does not select another probe replacement. The installed descriptor retains source plan `0x3bd2e4ea748c73df475937b81b722c4dd3306fb83ce6e5f29ef3444e0e8abd95`, CREATE nonce 34, and signed anchor block 7,986,580. The observer reuses that descriptor and freshly verifies current nonce/custody. Native write/restore actions remain the original actions ([plan.go:614](/home/by/urnetwork/sn/sim-testnet/plan.go:614), [plan_revision.go:4185](/home/by/urnetwork/sn/sim-testnet/plan_revision.go:4185), [plan_revision.go:4221](/home/by/urnetwork/sn/sim-testnet/plan_revision.go:4221), [fleet_renewal_revision.go:12](/home/by/urnetwork/sn/sim-testnet/fleet_renewal_revision.go:12), [evidence_carry.go:569](/home/by/urnetwork/sn/sim-testnet/evidence_carry.go:569), [precompile_probe_successor.go:548](/home/by/urnetwork/sn/sim-testnet/precompile_probe_successor.go:548)). Before apply, compare all 3,521 retained action IDs/intents and spends, all 1,212 renewal actions, the complete descriptor, policy/config/roles, cumulative spend and the unchanged 6,000 repair / 37,250 lifetime alpha / 180 EVM limits. Fresh chain facts can independently require a correction, so source review cannot claim the yet-unrendered plan is exactly equal. Normal admission archives the original plan bytes; do not rewrite receipts/journal ([executor.go:827](/home/by/urnetwork/sn/sim-testnet/executor.go:827)).

The narrow nginx rollout and source integration are separate events. Reloading reviewed nginx on the host while keeping the old 7eab/a9 source snapshots can leave the old lock, binary and 2b552 approval intact; that is the limited setting in which DEPLOYMENT.md's same-plan retry advice applies. Adopting xops51325 into the final repository snapshot requires the lock/revision sequence above. Conversely, updating the repository lock does not deploy nginx or prove the 429 condition is removed. The actual deployed config remains unverified because SSH authentication was unavailable; deployment and successful owned-endpoint evidence remain root's separate work.
