# FINAL-2 e982 preparation evidence

Intended repository destination: `sim-testnet/peerreview/evidence/FINAL-2-e982-preparation-20260914`.

This bundle contains concise public-safe records only. `references/RAW-HASH-REFERENCES.tsv` identifies excluded raw inputs and outputs by SHA-256. Verify the bundle with `sha256sum -c SHA256SUMS`.
