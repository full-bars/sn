# Exclusions

Not copied: the 5.9 MB setup-preview output, private plan/configuration contents, request JSON, keys, signed RLP, journals, raw stdout/stderr, executable binaries, caches, command launchers, live RPC material, and active native captures.

Only reviewed field hashes, source/binary identities, closed exits, state hashes/sizes, and concise derived result records are included.
