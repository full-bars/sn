# FINAL-2 probe reference lint evidence

Intended repository destination: `sim-testnet/peerreview/evidence/FINAL-2-probe-reference-lint-20260914`.

This compact external bundle records the closed strict-lint correction. It contains the deterministic source-to-control mapping, selected source and compiled membership, exact result tables, dependency and artifact identities, admission reconciliation, and the one expected causal lint failure. Verify retained content with `sha256sum -c SHA256SUMS`.

Raw logs, full source files, compiled artifacts, binaries, caches, temporary files, full commands, private operational material, and live gate records are not copied. Their closed identities are retained only in `references/RAW-HASH-REFERENCES.tsv`.

## Locator revision

This unpublished revision adds `references/RAW-LOCATORS.tsv`. It lists the original absolute capture roots and relative retained-record locations so reviewers can find the raw materials referenced by hash. It copies none of those materials.
