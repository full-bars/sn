# Exclusions

Not copied: source trees, compiled artifacts, executable binaries, caches, temporary directories, raw command streams, raw logs, keys, signed payloads, plans, configuration contents, live native material, RPC captures, and full-gate outputs. Closed raw materials are hash-referenced only.
