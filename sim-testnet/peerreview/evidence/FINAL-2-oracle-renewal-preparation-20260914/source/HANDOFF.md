The native renewal preview failed because it treated a completed oracle restore as a pending reroute. The correction is isolated in `sn/`, based on published SN `e31236859c1012c21aed08034cf5074d85bdb9a6`. Root owns publication, release-lock updates, native commands and chain/state writes. Terra owns formatting, tests and builds.

Final isolated commit: `536dba6541498238a339c958358ab1b4b50e8aac` (`Accept effective original oracle restores during fleet renewal`). The worktree is clean. Terra formatted exactly the three changed files and reported a clean `git diff --check`; qualification was handed over after this commit. Test/build outcomes belong to Terra's separate capture.

The failed preview is retained at `/home/by/urnetwork/temp/sn-renewal-before-repair-20260914/native-preview-e312-F359`. It exited 1 on 2026-09-14 at 06:48:23 UTC without stdout, transactions or changes to the four recorded state files. The original setup failure remains recorded separately: generation-2 `fleet.refresh.batch.1` was checked against the current generation-3 bindings installed by authenticated later lifecycle preparation for fleets 5 and 6.

Root's read-only owned-LAN oracle snapshot at 06:51:15 UTC pins EVM block 8002200, hash `0xefd219a0f812985c190ceda95241545617dceed9c6a06cd77d324b41d2f4e1eb`, epoch 356. Immutable, active and pending oracle addresses all equal the retained original `0x0422aC5e1EA3997D2A06f8268D4834380609738C`; pending epoch is 273. The RPC artifact SHA-256 is `e6029721531d2d601fef9db097187df7df702019d39546f0f0a87b426fa740dc`, under `/home/by/urnetwork/temp/sn-renewal-before-repair-20260914/oracle-current/`. Exact verified restore/await receipts are journal sequences 10152 and 10154.

`evm/src/STCoordinator.sol:656` rejects zero-address oracle schedules and stores the nonzero address/epoch. `activeCommitmentOracle()` at line 665 is a view: it selects the effective scheduled address and never clears those fields. Scheduling another restore cannot produce the zero/zero tuple demanded by the old renewal checks. No contract or chain-state change is needed to recognize the completed restore.

The production correction has only two files. `sim-testnet/fleet_renewal_observe.go` adds `fleetRenewalOriginalOracleReady` and uses it in planning. `sim-testnet/fleet_renewal_execute.go` uses that same predicate before each new renewal action. Both require a nonzero retained original address equal to immutable and active, plus either zero/zero pending fields or the same original pending address with a positive epoch at or before the observed current epoch. Future, foreign and inconsistent schedules remain refused. The validity-window, UID/coldkey, exact nonce, signed liability, custody, finality and spend checks remain in place.

`sim-testnet/fleet_renewal_oracle_test.go` reproduces the actual tuple through the existing production reader and a local fixture serving all five getters in one exact-block batch. It also checks the effective-epoch boundary, an unscheduled original oracle, future schedules, foreign active/immutable/pending identities, incomplete address/epoch pairs and zero/foreign retained identities. Root reviewed the shared-callsite coverage and requested no new full native/Substrate fixture.

Terra's bounded normal and race selector is:

```text
^(TestFleetRenewalOriginalOracleAcceptsCompletedRestore|TestFleetRenewalOriginalOracleRejectsChangedRouting|TestFleetRefreshOracleStateUsesOnePinnedBatch|TestFleetRenewalPlansExpiredAndLiveMixedGenerations|TestFleetRenewalRejectsChangedPrestateAndPreservesApproval|TestFleetRenewalRevisionPreservesApprovedRoundsAndChargesOnce|TestFleetRenewalPipelineSubmitsExactNoncesBeforeFinality)$
```

Run from the isolated module root with `./sim-testnet`; direct retained test-binary execution must use its `sim-testnet` package directory. The CLI build package is `./sim-testnet`. The actual native failed preview and pinned contract-state evidence establish the old failure; no additional causal source mutation is required. Root directs any repeated normal confirmations. No tests, builds or formatters were run by Astra.

The intended recovery is to qualify/publish this correction, admit the current locked setup plan natively if required, then plan and apply the already-needed native fleet renewal before retrying the approved 6000-alpha reserve repair/setup. Preserve the historical platform-config path and exact owned-LAN route on every native command. No public fallback or RPC pacing is authorized.

For root's native admission followed by graceful cancellation, `writeRunInputs` archives the prior plan and writes the new archive before atomically replacing `plan.json`, then writes `config.redacted.yml` and `public/identities.json`. SIGINT/SIGTERM reaches a context cancellation handler; join the actual native process so those synchronous writes and executor/journal defers complete. Verify the installed plan/archive and redacted lock/resolved hashes after exit. `setup` starts no managed dependencies or release binaries, and setup action execution starts only after the full carried audit. Record this interrupted command as canceled/not passed with repair pending; the later ordinary setup must complete the audit and repair normally.

Existing native renewal is appropriate for the stopped state: no `public/fleet-lifecycle.json` exists and no admitted provider, terminal or fallback lifecycle actions have begun. Its actions depend on their own renewal predecessors, not the failing carried setup audit. Renewal's doctor validates its remaining spend and immutable topology; it does not run the live reserve-majority barrier. The latter remains mandatory before launch. `fleetRenewalHistoricalSource` authenticates completed renewal successors and replays original refresh/lifecycle receipts at their original canonical checkpoints. `carryFleetRenewalRevision` preserves exact approved renewal actions and future lifecycle generations across later setup revisions, charging their gas once. This avoids any new lifecycle supersession mechanism or relaxation of the exact generation-2 version count.

Native renewal preview/apply still performs current custody, nonce, funding, budget and future-window checks. Any concrete refusal should be diagnosed from its retained output. This handoff does not claim those future commands have passed.
