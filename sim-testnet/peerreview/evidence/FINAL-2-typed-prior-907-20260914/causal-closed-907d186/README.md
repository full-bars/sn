# Closed 907d186 prior-carrier scheduling causal

This compact index supplements the sealed affected normal/race matrix at
`../matrix-closed-907d186`. It records the deterministic script-only control
for the original timeout: a disposable source keeps commit `907d186` and the
same compiled normal binary, while only the corrected producer script is
restored to its `fc908` form.

The causal body is an expected control: one named root fails and two controls
pass. It is not a candidate qualification failure. The body ran once after
pre-body metadata admission was corrected from two rows for one failure root
to one canonical literal containing both required fragments.

The earlier compact-runner and duplicate-literal launches are retained as
pre-body admissions. No test body, source, or binary changed in those attempts.
