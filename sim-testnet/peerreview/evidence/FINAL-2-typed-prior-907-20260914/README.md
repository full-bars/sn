# Typed-prior timeout correction: completed qualification

This portable copy preserves completed evidence for SN commit
`907d18698f3464da8193894a3ad42101184a6a56`. It does not claim execution on later
runtime-458 revisions.

The affected matrix passed all 26 roots normally and under race. The root
active at the original cumulative package timeout passed three consecutive
fresh race processes on unchanged binary bytes. The normal causal control
restored only the old producer script: its typed-owner guard failed as
expected, and both adjacent controls passed. See the two closure directories
for exact roots, modes, binary identities, raw locators and outcomes.

Raw run directories include their compiled-root lists, test events, actual
exits and source/dependency fences. Original absolute locators remain unchanged;
the enclosing SHA256SUMS provides relative checksums for the copied files.
The original failing full producer is preserved separately in
`sn-final2-producer-fc908-evidence-20260914`.

The first causal launcher refused a duplicate failure-literal input before a
test body ran. Its retained error is separate from the successful corrected
launcher and the intentionally failing causal test. Earlier capture admission
refusals remain at their original external paths, as recorded in the execution
checkpoint. No qualification was rerun to prepare this copy.

Original capture:
`/home/by/urnetwork/temp/sn-prior-carrier-capacity-correction-20260914/terra-runtime/qualification-typed-prior-907d186-streaming`.
