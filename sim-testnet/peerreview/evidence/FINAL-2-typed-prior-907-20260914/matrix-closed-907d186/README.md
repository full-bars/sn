# Closed 907d186 affected normal/race matrix

This index records only the accepted 25-guard plus one boundary root matrix.
Normal and race each have 26 disjoint top-level roots. The boundary race root
has three fresh sequential passes on the retained race binary. Each reviewed
body records the full 13-source-manifest fence, repository snapshot equality,
and unchanged binary hash before and after execution.

The retained compact-runner and early static-owner refusals occurred before any
body and remain outside this closure. The script-only expected-failure causal is
separate and is not claimed here.
