# FINAL-2 7eab release-preparation evidence

Intended repository destination: `sim-testnet/peerreview/evidence/FINAL-2-7eab-preparation-20260914`.

This external bundle records only closed release preparation: source publication, the reviewed one-field lock update, lock publication, final CLI identity, and the read-only setup preview. Verify retained files with `sha256sum -c SHA256SUMS`.

Raw logs, requests, full plan/configuration material, signed payloads, executable binaries, caches, live apply material, and full-gate records are excluded and retained by SHA-256 reference only.
