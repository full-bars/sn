# FINAL-2 evidence-render owner qualification

This sealed external bundle records two related snapshots without merging their outcomes.

- 1e491b6 is closed and failed: both normal and race 18-root guard bodies had 16 PASS and 2 real FAIL. Its launcher and checker-adapter incidents remain separate records.
- 713eae3 is the test-only AST source-census successor. The exact compiled 81-root normal and race matrices each closed with 81 top-level PASS, no failures, and matching 81-name lists.
- The two original guard failures have completed p1/p2/p3 normal and race confirmations on the retained mode binaries.
- The four original timeout roots have completed final-source race p1/p2/p3 on the retained race binary.
- The regex causal closes as expected at 3 FAIL / 3 PASS. The script-only causal closes as expected at 3 FAIL / 1 PASS.

These are bounded qualification results. This bundle does not assert a complete producer or aggregate gate pass, final publication, a final CLI result, deployment, a soak, native activity, or a live-node outcome.

The source/dependency identity is fixed in fences/SOURCE-DEPENDENCY-13.tsv. references/RAW-LOCATORS.tsv contains hashes and locators for retained raw material; raw test streams, binaries, caches, fixtures, plans, keys, and private state are not copied.
