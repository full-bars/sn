# Evidence-render owner and source-census qualification

This checksum-sealed bundle preserves the failed `1e491b6` qualification and completed `713eae3` correction as separate results. [SUMMARY.md](SUMMARY.md) describes the scope.

Both 81-root matrices passed. The two actual failed guards completed three consecutive executions in each failed mode, and all four timeout roots completed three race executions on the same corrected binary. The original-regex causal produced three failures with three passing controls; the original-script causal produced three failures with one passing control. The tables retain actual exits, identities, raw locators, earlier failures and capture-admission incidents.

[The obsolete aggregate closure](closures/OLD-AGGREGATE-CANCELLATION.tsv) records its cancellation and known original-source Solidity lint failure. [The infrastructure-lock pointer](references/INFRASTRUCTURE-LOCK-POINTER.tsv) refers to the separate local lock observation. These records do not establish a complete gate PASS, final CLI build, deployment, native apply, live soak or final acceptance.

The original external bundle remains at `/home/by/urnetwork/temp/sn-final2-evidence-render-owner-qualification-evidence-20260914`, with SHA256SUMS digest `3c17493ff02dd0a410a91ac66f2201a32117621e5188cd0810a6cb20faeb85e7`. During publication, root corrected this README's stale staging/pending wording and regenerated only the file manifest and checksum records. All qualification, source, result, closure and raw-locator files retain the original sealed bytes.
